<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>YourBoard - <%= pageTitle %></title>
  <link rel="stylesheet" href="/css/style.css">
  <!-- Chart.js para os gráficos -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  <div class="app-container">
    <!-- Sidebar -->
    <aside class="sidebar">
      <div class="logo">
        <h1>YourBoard</h1>
      </div>
      <nav class="main-nav">
        <ul>
          <li><a href="/dashboard" class="<%= currentPage === 'dashboard' ? 'active' : '' %>">Dashboard</a></li>
          <li><a href="/tarefas" class="<%= currentPage === 'tarefas' ? 'active' : '' %>">Tarefas</a></li>
          <li><a href="/agenda" class="<%= currentPage === 'agenda' ? 'active' : '' %>">Agenda</a></li>
          <li><a href="/graficos" class="<%= currentPage === 'graficos' ? 'active' : '' %>">Gráficos</a></li>
          <li><a href="/diario" class="<%= currentPage === 'diario' ? 'active' : '' %>">Diário</a></li>
          <li><a href="/atalhos" class="<%= currentPage === 'atalhos' ? 'active' : '' %>">Atalhos</a></li>
        </ul>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <header class="page-header">
        <h2><%= pageTitle %></h2>
      </header>
      
      <div class="content">
        <%- body %>
      </div>
    </main>
  </div>

  <!-- Scripts específicos da página -->
  <% if (typeof pageScript !== 'undefined') { %>
    <script src="/js/<%= pageScript %>.js"></script>
  <% } %>
</body>
</html>
